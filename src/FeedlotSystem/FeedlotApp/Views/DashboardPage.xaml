<ContentPage
    x:Class="FeedlotApp.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:FeedlotApp.ViewModels"
    Title="Dashboard">

    <ContentPage.BindingContext>
        <vm:DashboardViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="25">

            <Label Text="📊 Dashboard" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center" />

            <Label Text="Animals:" FontSize="18" />
            <Label Text="{Binding AnimalCount}" FontSize="30" FontAttributes="Bold" TextColor="Green" />

            <Label Text="Bookings:" FontSize="18" />
            <Label Text="{Binding BookingCount}" FontSize="30" FontAttributes="Bold" TextColor="Blue" />

            <Button Text="🔄 Refresh" Command="{Binding RefreshCommand}" BackgroundColor="#6200EE" TextColor="White" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
